uboot:
  size : @@UBOOT_ENV_SIZE@@
  lockfile : /var/lock/fw_printenv.lock
  devices:
    - path : @@UBOOT_STORAGE_DEVICE@@
      offset : @@UBOOT_ENV_ADDR@@
    - path : @@UBOOT_STORAGE_DEVICE@@
      offset : @@UBOOT_ENV_ADDR_REDUND@@
